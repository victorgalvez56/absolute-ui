/**
 * Tailwind CSS v4 integrates with PostCSS via its dedicated plugin.
 * No additional plugins are needed — Tailwind 4 ships with its own
 * Lightning CSS pipeline.
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
