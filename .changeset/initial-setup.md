---
---

Initial changesets pipeline setup. This empty changeset documents that the
Changesets release pipeline is wired up for `@absolute-ui/tokens`,
`@absolute-ui/a11y`, and `@absolute-ui/core`. It will be dropped on the
first real release.
